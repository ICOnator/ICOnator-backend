server.port=8082

spring.profiles.active=prod
spring.main.banner-mode=off

spring.application.name=iconator-monitor
logging.level.io.iconator.monitor=DEBUG

management.health.rabbit.enabled=false

io.iconator.commons.amqp.url=${AMQP_URL}

io.iconator.commons.bitcoin.network=${BITCOIN_NETWORK}

io.iconator.monitor.btc.node.enabled=${MONITOR_BITCOIN_NODE_ENABLED}

io.iconator.monitor.eth.node.enabled=${MONITOR_ETHEREUM_NODE_ENABLED}
io.iconator.monitor.eth.node.start-block=${MONITOR_ETHEREUM_NODE_START_BLOCK}
io.iconator.monitor.eth.node.url=${MONITOR_ETHEREUM_NODE_URL}